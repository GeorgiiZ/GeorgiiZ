export class Monument {
  constructor({ id, name, photo, region, address }) {
    this.id = id
    this.name = name
    this.photo = photo
    this.region = region
    this.address = address
  }
}